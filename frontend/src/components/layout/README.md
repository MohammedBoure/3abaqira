# Layout Components

This directory contains application shell and navigation layout components.

## Files

- **`Navbar.jsx`**: Sticky top navigation bar featuring the brand logo, multi-tenant branch selector (`CENTER` ⇄ `RAWDA`), global search bar, language toggle (`العربية` / `Français`), notifications bell, and user profile avatar.
