# Mock Data Layer (`frontend/src/mock/`)

This directory contains static, structured mock datasets powering the non-practical visual prototype of the 3abaqira Enterprise Management Platform.

## Directory Contents

| File | Purpose |
| :--- | :--- |
| [mockData.js](file:///C:/Users/moham/Desktop/3abaqira/frontend/src/mock/mockData.js) | Structured mock entities including multi-tenant branches (`MOCK_BRANCHES`, `MOCK_BRANCHES_DETAILED`), operational KPI metrics (`MOCK_METRICS`), comprehensive multi-column student roster (`MOCK_STUDENTS_ROSTER` with 26 attributes), educational programs (`MOCK_PROGRAMS`), daily cash register transactions (`MOCK_CASH_DRAWER_TRANSACTIONS`), staff payroll records (`MOCK_PAYROLL_DATA`), cafeteria provisions logs (`MOCK_PROVISIONS_DATA`), monthly analytics metrics (`MOCK_ANALYTICS_DATA`), academic cycles (`MOCK_ACADEMIC_YEARS`), audit logs (`MOCK_AUDIT_LOGS`, `MOCK_AUDIT_STATS`), authentication & identity (`MOCK_CURRENT_USER`, `MOCK_AUTH_USERS`), invoices & receipts (`MOCK_INVOICES`, `MOCK_PAYMENTS_LIST`), expense records & budget variances (`MOCK_EXPENSES_LIST`, `MOCK_EXPENSE_CATEGORIES`, `MOCK_BUDGET_VARIANCES`), cash handovers (`MOCK_HANDOVERS`), pricing plans & tariffs (`MOCK_PRICING_PLANS`), academic enrollments (`MOCK_ENROLLMENTS`), groups & levels (`MOCK_LEVELS_LIST`, `MOCK_GROUPS_LIST`), timetables & sessions (`MOCK_SCHEDULES`, `MOCK_SESSIONS_LIST`), competitions & candidate registrations (`MOCK_COMPETITIONS`, `MOCK_COMPETITION_REGISTRATIONS`), parents directory (`MOCK_GUARDIANS`), and system telemetry (`MOCK_SYSTEM_TELEMETRY`). |
| [deliveryLedgerData.js](file:///C:/Users/moham/Desktop/3abaqira/frontend/src/mock/deliveryLedgerData.js) | Annual delivery ledger calendar dataset (`MOCK_DELIVERY_LEDGER`, `DELIVERY_MONTHS`, `computeDeliveryLedgerStats`, `generateInitialDeliveryLedger`) structuring 334 days across 11 months (September 2025 to July 2026) with receipt numbers, amounts, recipients, remarks, and monthly subtotal recalculation logic. |
| [expensesLedgerData.js](file:///C:/Users/moham/Desktop/3abaqira/frontend/src/mock/expensesLedgerData.js) | Annual daily expenses ledger calendar dataset (`MOCK_EXPENSES_LEDGER`, `EXPENSES_MONTHS`, `COMMON_DESIGNATIONS`, `computeExpensesLedgerStats`, `generateInitialExpensesLedger`) structuring 334 days across 11 months (September 2025 to July 2026) with dates, designations (التعيين), amounts (المبلغ), and dynamic monthly subtotal recalculation logic. |
| [rawdaMockData.js](file:///C:/Users/moham/Desktop/3abaqira/frontend/src/mock/rawdaMockData.js) | Kindergarten & Daycare datasets conforming directly to `docs/frontend.md`: students roster with 11 months and annual offers (`MOCK_RAWDA_STUDENTS`), 10 rooms kanban cohorts (`MOCK_RAWDA_ROOMS`), daily operational expenses (`MOCK_RAWDA_DAILY_EXPENSES`), 7-category budget variance analysis (`MOCK_RAWDA_BUDGET_VARIANCE`), daily cash register reconciliation (`MOCK_RAWDA_CASH_DRAWER`), cash handovers (`MOCK_RAWDA_CASH_HANDOVER`), weekly bread consumption logs (`MOCK_RAWDA_BREAD_TRACKING`), and weekly meat/food provisions (`MOCK_RAWDA_MEAT_PROVISIONS`). |
| [centerMockData.js](file:///C:/Users/moham/Desktop/3abaqira/frontend/src/mock/centerMockData.js) | Academic Center datasets conforming to `docs/frontend.md`: 4-installment programs (`MOCK_CENTER_PROGRAMS`), bilingual Soroban mental math roster with belts and level codes (`MOCK_CENTER_SOROBAN`), 6-month semester Quran memorization (`MOCK_CENTER_QURAN`), 9-month preparatory school registry (`MOCK_CENTER_PREPARATORY`), 2-installment summer camp (`MOCK_CENTER_SUMMER_CAMP`), championships registry (`MOCK_CENTER_CHAMPIONSHIPS`), weekly schedule matrix (`MOCK_CENTER_TIMETABLE`), 11-month fixed staff payroll (`MOCK_CENTER_FIXED_PAYROLL`), variable trainer payroll calculator (`MOCK_CENTER_TRAINERS`), center daily expenses (`MOCK_CENTER_DAILY_EXPENSES`), cash handovers (`MOCK_CENTER_CASH_HANDOVER`), and discount policy matrix (`MOCK_CENTER_PRICING_POLICY`). |

