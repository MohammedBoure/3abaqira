# Mock Data Layer (`frontend/src/mock/`)

This directory contains static, structured mock datasets powering the non-practical visual prototype of the 3abaqira Enterprise Management Platform.

## Directory Contents

| File | Purpose |
| :--- | :--- |
| [mockData.js](file:///C:/Users/moham/Desktop/3abaqira/frontend/src/mock/mockData.js) | Structured mock entities including multi-tenant branches (`MOCK_BRANCHES`), operational KPI metrics (`MOCK_METRICS`), comprehensive multi-column student roster (`MOCK_STUDENTS_ROSTER` with 26 attributes), educational programs (`MOCK_PROGRAMS`), daily cash register transactions (`MOCK_CASH_DRAWER_TRANSACTIONS`), staff payroll records (`MOCK_PAYROLL_DATA`), cafeteria provisions logs (`MOCK_PROVISIONS_DATA`), and monthly analytics metrics (`MOCK_ANALYTICS_DATA`). |

