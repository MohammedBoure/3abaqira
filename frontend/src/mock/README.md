# Mock Data Layer (`frontend/src/mock/`)

This directory contains static, structured mock datasets powering the non-practical visual prototype of the 3abaqira Enterprise Management Platform.

## Directory Contents

| File | Purpose |
| :--- | :--- |
| [mockData.js](file:///C:/Users/moham/Desktop/3abaqira/frontend/src/mock/mockData.js) | Structured mock entities including multi-tenant branches (`MOCK_BRANCHES`, `MOCK_BRANCHES_DETAILED`), operational KPI metrics (`MOCK_METRICS`), comprehensive multi-column student roster (`MOCK_STUDENTS_ROSTER` with 26 attributes), educational programs (`MOCK_PROGRAMS`), daily cash register transactions (`MOCK_CASH_DRAWER_TRANSACTIONS`), staff payroll records (`MOCK_PAYROLL_DATA`), cafeteria provisions logs (`MOCK_PROVISIONS_DATA`), monthly analytics metrics (`MOCK_ANALYTICS_DATA`), academic cycles (`MOCK_ACADEMIC_YEARS`), audit logs (`MOCK_AUDIT_LOGS`, `MOCK_AUDIT_STATS`), authentication & identity (`MOCK_CURRENT_USER`, `MOCK_AUTH_USERS`), invoices & receipts (`MOCK_INVOICES`, `MOCK_PAYMENTS_LIST`), expense records & budget variances (`MOCK_EXPENSES_LIST`, `MOCK_EXPENSE_CATEGORIES`, `MOCK_BUDGET_VARIANCES`), cash handovers (`MOCK_HANDOVERS`), pricing plans & tariffs (`MOCK_PRICING_PLANS`), academic enrollments (`MOCK_ENROLLMENTS`), groups & levels (`MOCK_LEVELS_LIST`, `MOCK_GROUPS_LIST`), timetables & sessions (`MOCK_SCHEDULES`, `MOCK_SESSIONS_LIST`), competitions & candidate registrations (`MOCK_COMPETITIONS`, `MOCK_COMPETITION_REGISTRATIONS`), parents directory (`MOCK_GUARDIANS`), and system telemetry (`MOCK_SYSTEM_TELEMETRY`). |
